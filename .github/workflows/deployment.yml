name: Dummy Deployment

on:
  create:
    tags:
      - "v**"

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Dummy Deployment
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Hello world
        run: echo "Hello world"
