name: Deployment

on:
  push:
    tags:
      - "v**"

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: SVA ubuntu check
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Hello world
        run: echo "Hello world"
